[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "diffcoast"
version = "0.1.0"
description = "Differentiable GPU-native coastal ocean model"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "jax",
    "jaxlib",
    "matplotlib",
]
