# DiffCoast

A differentiable, GPU‑native coastal ocean model written in Python using JAX.

## Features
- 2D depth‑integrated shallow water equations
- Wind forcing and closed boundaries
- Automatic differentiation for sensitivity analysis
- Runs on CPU or GPU (via JAX)

## Installation
```bash
pip install git+https://github.com/yourusername/diffcoast.git
